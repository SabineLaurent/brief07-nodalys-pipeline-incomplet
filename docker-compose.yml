services:
  db:
    image: postgres:16
    environment:
      POSTGRES_USER: nodalys
      POSTGRES_PASSWORD: nodalys
      POSTGRES_DB: nodalys
    ports:
      - "5432:5432"
    volumes:
      - nodalys_db_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U nodalys -d nodalys"]
      interval: 5s
      timeout: 5s
      retries: 10

  mock_api:
    build: ./mock_api
    ports:
      - "8001:8001"
    environment:
      MOCK_API_PORT: "8001"
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8001/health')"]
      interval: 5s
      timeout: 5s
      retries: 10

  cron:
    build:
      context: .
      dockerfile: Dockerfile.cron
    depends_on:
      db:
        condition: service_healthy
    environment:
      DB_URL: postgresql+psycopg://nodalys:nodalys@db:5432/nodalys

volumes:
  nodalys_db_data:
